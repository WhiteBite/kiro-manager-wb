# 📋 TODO: Рефакторинг кодовой базы

**Статус:** ✅ ВСЕ КРИТИЧЕСКИЕ И СРЕДНИЕ ЗАДАЧИ ЗАВЕРШЕНЫ  
**Дата начала:** 4 января 2026  
**Дата завершения:** 4 января 2026

---

## ✅ ЗАВЕРШЁННЫЕ ЗАДАЧИ

### TASK-001: Удалить дублирование CSS переменных ✅
- **Статус:** ✅ Готово
- **Результат:** Файл `styles.ts` сократился с 348 до 14 строк. Удалён весь блок `legacyStyles`.

---

### TASK-002: Вынести `is_kiro_running()` в общий модуль ✅
- **Статус:** ✅ Готово
- **Результат:** 
  - Создан `autoreg/core/process_utils.py` с функциями `is_kiro_running()` и `kill_kiro()`
  - Обновлены 4 файла: `kiro_service.py`, `machine_id_service.py`, `kiro_patcher_service.py`, `test_patches.py`
  - Удалены все дублирующиеся методы `_is_kiro_running()` / `_is_running()`

---

### TASK-003: Унифицировать `escapeHtml()` ✅
- **Статус:** ✅ Готово
- **Результат:** Функция в `scripts.ts` переименована в `escapeHtmlClient()` с комментарием объясняющим разницу между серверной и клиентской версиями.

---

### TASK-004: Консолидировать пути Kiro в `paths.py` ✅
- **Статус:** ✅ Готово
- **Результат:**
  - Добавлено свойство `kiro_install_path` в класс `Paths`
  - Функция `get_kiro_install_path()` в `kiro_config.py` теперь использует централизованные пути
  - Удалено ~30 строк дублирующегося кода

---

### TASK-005: Разбить `layout.ts` на компоненты ✅
- **Статус:** ✅ Готово
- **Результат:**
  - Создана папка `src/webview/styles/layout/` с 17 файлами
  - Модули: `header.ts`, `hero.ts`, `toolbar.ts`, `account-list.ts`, `modals.ts`, `settings.ts`, `logs.ts`, `tabs.ts`, `toast.ts`, `skeleton.ts`, `autoreg.ts`, `settings-card.ts`, `stats.ts`, `strategy.ts`, `batch-reg.ts`, `responsive.ts`, `index.ts`
  - Обратная совместимость сохранена

---

### TASK-006: Централизовать типы TypeScript ✅
- **Статус:** ✅ Готово
- **Результат:**
  - Добавлен реэкспорт общих типов в `src/webview/types.ts`
  - Удалены дублирующиеся определения `ImapProfile` из `Hero.ts` и `index.ts`
  - Все типы теперь централизованы

---

### TASK-007: Расширить ProcessManager ✅
- **Статус:** ✅ Готово
- **Результат:**
  - Добавлены функции: `is_process_running()`, `wait_for_process_exit()`, `kill_process()`
  - Рефакторинг `is_kiro_running()` и `kill_kiro()` для использования общих функций
  - Обновлён `autoreg/core/__init__.py` для экспорта всех функций

---

### TASK-008: Создать файл констант Python ✅
- **Статус:** ✅ Готово
- **Результат:**
  - Создан `autoreg/core/constants.py` с 20+ константами
  - Обновлён `autoreg/core/__init__.py` для экспорта констант
  - Обновлён `token_service.py` для использования констант

---

### TASK-009: Удалить неиспользуемые импорты ✅
- **Статус:** ✅ Готово
- **Результат:**
  - Удалён `import subprocess` из `machine_id_service.py`
  - Все остальные файлы проверены - импорты используются

---

## ✅ НИЗКИЕ ЗАДАЧИ (завершены)

### TASK-010: Унифицировать стиль комментариев ✅
- **Статус:** ✅ Готово
- **Результат:** Унифицированы комментарии в 14 TypeScript файлах и 2 Python файлах. Стиль: JSDoc для экспортов, `/* === Section === */` для CSS секций, `# === Section ===` для Python.

### TASK-011: Добавить JSDoc к публичным функциям ✅
- **Статус:** ✅ Готово
- **Результат:** Добавлена JSDoc документация с @param, @returns, @example к функциям в `helpers.ts`, `scripts.ts`, `styles/index.ts`, `styles/layout/index.ts`.

### TASK-012: Заменить магические числа в CSS на переменные ✅
- **Статус:** ✅ Готово
- **Результат:** Заменены магические числа на CSS переменные в 14 файлах layout/. Примеры: `12px` → `var(--space-3)`, `13px` → `var(--font-size-base)`.

---

## 📊 Прогресс

| Категория | Всего | Готово | В работе | Ожидает |
|-----------|-------|--------|----------|---------|
| 🔴 Критические | 6 | **6** | 0 | 0 |
| 🟠 Средние | 3 | **3** | 0 | 0 |
| 🟡 Низкие | 3 | **3** | 0 | 0 |
| **Итого** | **12** | **12** | **0** | **0** |

### ✅ ВСЕ ЗАДАЧИ ЗАВЕРШЕНЫ
- 12 из 12 задач выполнены
- Код компилируется без ошибок
- Дублирование устранено
- Комментарии унифицированы
- JSDoc документация добавлена
- CSS переменные используются везде

---

## 📁 Созданные файлы

| Файл | Описание |
|------|----------|
| `autoreg/core/process_utils.py` | Общие функции для работы с процессами |
| `autoreg/core/constants.py` | Централизованные константы |

## 📝 Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `src/webview/styles.ts` | Удалён legacyStyles (348→14 строк) |
| `src/webview/scripts.ts` | `escapeHtml` → `escapeHtmlClient` |
| `src/webview/types.ts` | Добавлен реэкспорт типов |
| `autoreg/core/paths.py` | Добавлен `kiro_install_path` |
| `autoreg/core/kiro_config.py` | Использует централизованные пути |
| `autoreg/core/__init__.py` | Экспорт констант |
| `autoreg/services/kiro_service.py` | Использует `process_utils` |
| `autoreg/services/machine_id_service.py` | Использует `process_utils` |
| `autoreg/services/kiro_patcher_service.py` | Использует `process_utils` |
| `autoreg/services/token_service.py` | Использует константы |

---

*Последнее обновление: 4 января 2026*
