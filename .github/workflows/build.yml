name: Build & Release

on:
  push:
    branches: [main, master]
    tags:
      - "v*"
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: TypeScript check
        run: npx tsc --noEmit

  build-vsix:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: win32-x64
            artifact: vsix-win32-x64
          - os: ubuntu-latest
            target: linux-x64
            artifact: vsix-linux-x64
          - os: macos-14
            target: darwin-arm64
            artifact: vsix-darwin-arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Node dependencies
        run: npm ci

      - name: Get version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          npm version $VERSION --no-git-tag-version --allow-same-version

      - name: Build extension
        run: npm run build

      - name: Install Python dependencies
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r autoreg/requirements.txt

      - name: Build bundled executables
        if: startsWith(github.ref, 'refs/tags/v')
        run: npm run build:executable

      - name: Package VSIX
        shell: bash
        run: |
          npm install -g @vscode/vsce
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          vsce package --no-dependencies --target ${{ matrix.target }} --out "kiro-manager-wb-${PACKAGE_VERSION}-${{ matrix.target }}.vsix"

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: "kiro-manager-wb-*.vsix"
          retention-days: 30

  build-standalone:
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: windows
            ext: .exe
            asset_name: kiro-manager-windows
          - os: ubuntu-latest
            name: linux
            ext: ""
            asset_name: kiro-manager-linux
          - os: macos-14
            name: macos-arm64
            ext: ""
            asset_name: kiro-manager-macos-arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Node dependencies
        run: npm ci

      - name: Build standalone HTML
        run: npm run build:standalone

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r autoreg/requirements.txt

      - name: Build with PyInstaller
        run: |
          cd autoreg
          pyinstaller kiro-manager.spec
        shell: bash

      - name: Get version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Rename artifact
        run: |
          cd autoreg/dist
          VERSION="${{ steps.version.outputs.version }}"
          if [ "${{ matrix.ext }}" = ".exe" ]; then
            mv kiro-manager.exe ${{ matrix.asset_name }}-${VERSION}.exe
          else
            mv kiro-manager ${{ matrix.asset_name }}-${VERSION}
          fi
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: autoreg/dist/${{ matrix.asset_name }}-${{ steps.version.outputs.version }}${{ matrix.ext }}
          retention-days: 30

  build-python:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version
        id: version
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Create Python bundle
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ZIP_NAME="kiro-manager-python-${VERSION}.zip"
          zip -r "$ZIP_NAME" autoreg \
            -x "autoreg/**/__pycache__/*" \
               "autoreg/**/.pytest_cache/*" \
               "autoreg/**/dist/*" \
               "autoreg/**/build/*" \
               "autoreg/**/.venv/*" \
               "autoreg/**/venv/*" \
               "autoreg/**/debug_sessions/*"
          ls -la "$ZIP_NAME"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: kiro-manager-python
          path: "kiro-manager-python-*.zip"
          retention-days: 30

  release:
    needs: [build-vsix, build-standalone, build-python]
    runs-on: ubuntu-latest
    if: always() && startsWith(github.ref, 'refs/tags/v') && needs.build-vsix.result == 'success'
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dl-artifacts

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: List artifacts
        run: find dl-artifacts -type f || true

      - name: Flatten artifacts
        run: |
          mkdir -p /tmp/release-files
          find dl-artifacts -type f -exec cp {} /tmp/release-files/ \;
          ls -la /tmp/release-files/

      - name: Checkout for changelog
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate changelog
        id: changelog
        run: |
          # Get previous tag
          PREV_TAG=$(git describe --tags --abbrev=0 ${{ github.ref_name }}^ 2>/dev/null || echo "")
          
          if [ -n "$PREV_TAG" ]; then
            echo "Previous tag: $PREV_TAG"
            COMMITS=$(git log --pretty=format:"- %s" $PREV_TAG..${{ github.ref_name }} --no-merges | head -20)
          else
            COMMITS=$(git log --pretty=format:"- %s" -10 --no-merges)
          fi
          
          # Save to file for multiline
          echo "$COMMITS" > changelog.txt
          echo "commits<<EOF" >> $GITHUB_OUTPUT
          cat changelog.txt >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: /tmp/release-files/*
          fail_on_unmatched_files: false
          body: |
            ## ⚡ Kiro Manager WB ${{ steps.version.outputs.version }}
            
            Расширение для управления аккаунтами Kiro IDE с авторегистрацией.
            
            ### 📦 Downloads
            
            | Platform | File | Description |
            |----------|------|-------------|
            | 🔌 VS Code / Kiro (Windows) | `kiro-manager-wb-${{ steps.version.outputs.version }}-win32-x64.vsix` | Расширение + bundled bin |
            | 🔌 VS Code / Kiro (Linux) | `kiro-manager-wb-${{ steps.version.outputs.version }}-linux-x64.vsix` | Расширение + bundled bin |
            | 🔌 VS Code / Kiro (macOS arm64) | `kiro-manager-wb-${{ steps.version.outputs.version }}-darwin-arm64.vsix` | Расширение + bundled bin |
            | 🪟 Windows | `kiro-manager-windows-${{ steps.version.outputs.version }}.exe` | Standalone авторег |
            | 🐧 Linux | `kiro-manager-linux-${{ steps.version.outputs.version }}` | Standalone авторег |
            | 🍎 macOS | `kiro-manager-macos-arm64-${{ steps.version.outputs.version }}` | Standalone авторег (Apple Silicon) |
            | 🐍 Python (all platforms) | `kiro-manager-python-${{ steps.version.outputs.version }}.zip` | Исходники autoreg + requirements |
            
            ### 🔧 Installation
            
            **VS Code / Kiro Extension:**
            ```
            Ctrl+Shift+P → Extensions: Install from VSIX → выбрать .vsix файл
            ```
            
            **Standalone (Windows):**
            ```
            kiro-manager-windows-${{ steps.version.outputs.version }}.exe --help
            ```

            **Python (all platforms):**
            ```
            unzip kiro-manager-python-${{ steps.version.outputs.version }}.zip
            cd autoreg
            python -m pip install -r requirements.txt
            python cli.py --help
            ```
            
            ### 📝 Changes
            
            ${{ steps.changelog.outputs.commits }}
            
            ---
            
            📢 **Telegram:** [@whitebite_devsoft](https://t.me/whitebite_devsoft)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send Telegram notification
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_THREAD_ID: ${{ secrets.TELEGRAM_THREAD_ID }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          CHANGELOG=$(cat changelog.txt | head -10 | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')
          MESSAGE="🚀 *Kiro Manager WB ${VERSION}*\n\n📦 Новый релиз доступен!\n\n📝 *Изменения:*\n${CHANGELOG}\n\n🔗 [Скачать на GitHub](https://github.com/WhiteBite/kiro-manager-wb/releases/tag/${VERSION})"
          
          # Build JSON with optional message_thread_id for forum topics
          if [ -n "${TELEGRAM_THREAD_ID}" ]; then
            JSON_DATA="{\"chat_id\": \"${TELEGRAM_CHAT_ID}\", \"message_thread_id\": ${TELEGRAM_THREAD_ID}, \"text\": \"${MESSAGE}\", \"parse_mode\": \"Markdown\", \"disable_web_page_preview\": true}"
          else
            JSON_DATA="{\"chat_id\": \"${TELEGRAM_CHAT_ID}\", \"text\": \"${MESSAGE}\", \"parse_mode\": \"Markdown\", \"disable_web_page_preview\": true}"
          fi
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "${JSON_DATA}"

  # Auto-update "latest" release on every master push (VSIX only)
  latest-release:
    needs: build-vsix
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download VSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: vsix
          path: artifacts

      - name: Update Latest Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest Build"
          body: |
            🚀 **Automatic build from master branch**

            Commit: ${{ github.sha }}
            Date: ${{ github.event.head_commit.timestamp }}

            This release is automatically updated on every push to master.
            For stable releases with standalone executables, see tagged versions.
          files: artifacts/*.vsix
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
